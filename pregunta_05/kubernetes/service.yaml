apiVersion: v1
kind: Service
metadata:
  name: observability-api-service
  namespace: default
  labels:
    app: observability-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  selector:
    app: observability-api
